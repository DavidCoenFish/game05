move output formatting to arrays in AST node, rather than make new ast nodes for formatting,
ie, new line, depth, whitespace

return ast type to just statement, and add a subtype "constructor, detructor, member, method, ..."

